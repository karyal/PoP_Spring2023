package week9.day2;

public class ManageLogin {
	public User login(User user) {
		//receive user info from UI
		//send user info to JDBC class
		//receive login status from JDBC class
		//return login status to UI
		return new User();
	}
}
